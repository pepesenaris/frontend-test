var React = require('react');

var EventsHeader = React.createClass({

  render: function() {
    return (
      <header>
        <h1>Events</h1>
      </header>
    );
  }
});

module.exports = EventsHeader;
